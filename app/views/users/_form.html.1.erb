<%= form_with(model: user, local: true) do |form| %>

  <div class="field">
    <%= form.label :image %>
    <%= form.file_field :profile_image, id: :feed_image %>
    <%= form.hidden_field :image_cache %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
